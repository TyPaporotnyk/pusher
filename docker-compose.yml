version: '3.9'

services:
  common:
    build:
      context: ./
      dockerfile: Dockerfile
    env_file:
      - .env
    restart: on-failure
    network_mode: host
    depends_on:
      - redis

  web:
    extends:
      service: common
    command: poetry run python manage.py runserver 0.0.0.0:8080

  bot:
    extends:
      service: common
    command: poetry run python manage.py bot

#  celery:
#    extends:
#      service: common
#    command: poetry run python -m celery -A config worker -l INFO
#
#  celery-beat:
#    extends:
#      service: common
#    command: poetry run python -m celery -A config beat -l INFO

  redis:
    image: redis:6
    restart: on-failure
